<div class="container">
  <h1> All Climates </h1>

  <table id="climates-table" class="table table-hover">
    <thead>
    <tr>
      <th>Name</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>

  <tbody>
    <% @styles.each do |style| %>
      <tr>
      <td><%= style.name %></td>
      <td><%= %></td>
      <td><%= %></td>
      </tr>
    <% end %>
  </tbody>
  </table>
</div>

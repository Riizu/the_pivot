<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" id="logo" href="/">Weekend at Chewie's</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right" id="navbar-links">
        <li><%= link_to "Cart (#{@cart.contents.count})", cart_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Planets<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% planets.each do |planet| %>
              <li><%= link_to planet.name, "/planets/#{planet.slug}" %></li>
            <% end %>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Climates<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% styles.each do |style| %>
              <li><%= link_to style.name, "/styles/#{style.slug}" %></li>
            <% end %>
          </ul>
        </li>
        <% if current_admin? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.first_name if current_user %><span class="caret"></span></a>
          <ul class="dropdown-menu">
              <li><%= link_to "Logout", "/logout", method: :delete %></li>
              <li><%= link_to "Dashboard", "/admin/dashboard" %></li>
          </ul>
        <% elsif current_user %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.first_name if current_user %><span class="caret"></span></a>
          <ul class="dropdown-menu">
              <li><%= link_to "Logout", "/logout", method: :delete %></li>
              <li><%= link_to "Dashboard", "/dashboard" %></li>
          </ul>
        </li>
        <% end %>
        <% user_button_display.each do |button| %>
          <li><%= button %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

require 'rails_helper'

RSpec.feature "User can checkout" do
  context "valid" do
    scenario "redirected to order show page" do
      #from the cart, which has x number of possible reservations
      #when they click
    end
  end
end
